# Bucket Sort

#### Algorithms

1. Create a bucket for a interval of values.

2. Sort items in each bucket using Collections.sort

3. Iterate all buckets to get sorted array.

Time Complexity: O(n + m + m * n/m(log(n/m)))  m is num of buckets

Space Complexity: O(m + n)



